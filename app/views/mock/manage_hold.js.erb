$('#holds_list').html("<%= escape_javascript(render partial: 'hold_list', :locals => {:holds => @holds}) %>");
sessionStorage.setItem('holds', "<%= escape_javascript(@updated_details['holds']) %>");
sessionStorage.setItem('holds_ready', "<%= escape_javascript(@updated_details['holds_ready']) %>");
update_login();
hold_management_binds();
